# coding-functions-02
# Hands-on Coding using Functions — Part 2

## More Practice

---

##  square

Écris une fonction appelée **`square`** qui prend un nombre comme paramètre, calcule son carré et retourne le résultat.

```javascript
function square(number) {
  // Write your code here
}

square(2); // -> 4
square(5); // -> 25
```

---

## cube

Écris une fonction appelée **`cube`** qui prend un nombre comme paramètre, calcule son cube et retourne le résultat.

```javascript
function cube(number) {
  // Write your code here
}

cube(2); // -> 8
cube(3); // -> 27
```

---

## charAtIndex

Écris une fonction appelée **`charAtIndex`** qui prend une chaîne de caractères et un nombre comme paramètres, puis retourne le caractère situé à l’index donné.

 *Indice : utilise `.charAt()`.*

```javascript
function charAtIndex(string, number) {
  // Write your code here
}

charAtIndex("Hello", 2); // -> "l"
charAtIndex("World", 0); // -> "W"
```

---

## addFourNums

Écris une fonction appelée **`addFourNums`** qui prend quatre nombres comme paramètres et retourne leur somme.

```javascript
function addFourNums(num1, num2, num3, num4) {
  // Write your code here
}

addFourNums(1, 1, 1, 1); // -> 4
addFourNums(1, 2, 3, 4); // -> 10
```

---

##  perimeterRect

Écris une fonction appelée **`perimeterRect`** qui prend la largeur et la longueur d’un rectangle et retourne son périmètre.

```javascript
function perimeterRect(width, length) {
  // Write your code here
}
```

---

##  areaRect

Écris une fonction appelée **`areaRect`** qui prend la largeur et la longueur d’un rectangle et retourne son aire.

```javascript
function areaRect(width, length) {
  // Write your code here
}
```

---

##  perimeterTriangle

Écris une fonction appelée **`perimeterTriangle`** qui prend les longueurs des trois côtés d’un triangle et retourne son périmètre.

```javascript
function perimeterTriangle(side1, side2, side3) {
  // Write your code here
}
```

---

##  areaTriangle

Écris une fonction appelée **`areaTriangle`** qui prend la base et la hauteur d’un triangle et retourne son aire.

```javascript
function areaTriangle(base, height) {
  // Write your code here
}
```

---

##  tripleArea

Écris une fonction appelée **`tripleArea`** qui prend la largeur et la longueur d’un rectangle, calcule son aire et retourne cette aire multipliée par **3**.

```javascript
function tripleArea(width, length) {
  // Write your code here
}
```

---

#  Advanced

##  perimeterOfCircle

Écris une fonction appelée **`perimeterOfCircle`** qui prend le rayon d’un cercle et retourne son périmètre.

 *Indice : utilise `Math.PI`.*

```javascript
function perimeterOfCircle(radius) {
  // Write your code here
}

perimeterOfCircle(3); // -> 18.85
perimeterOfCircle(5); // -> 31.42
perimeterOfCircle(8); // -> 50.27
```

---

## areaOfCircle

Écris une fonction appelée **`areaOfCircle`** qui prend le rayon d’un cercle et retourne son aire.

```javascript
function areaOfCircle(radius) {
  // Write your code here
}

areaOfCircle(3); // -> 28.27
areaOfCircle(5); // -> 78.54
areaOfCircle(8); // -> 201.06
```

---

##  inchesToCentimeters

Écris une fonction appelée **`inchesToCentimeters`** qui prend une longueur en pouces et retourne l’équivalent en centimètres.

```javascript
function inchesToCentimeters(inches) {
  // Write your code here
}

inchesToCentimeters(3);  // -> 7.62
inchesToCentimeters(4);  // -> 10.16
inchesToCentimeters(10); // -> 25.4
```

---

##  centimetersToInches

Écris une fonction appelée **`centimetersToInches`** qui prend une longueur en centimètres et retourne l’équivalent en pouces.

```javascript
function centimetersToInches(centimeters) {
  // Write your code here
}

centimetersToInches(10); // -> 3.93701
centimetersToInches(12); // -> 4.72441
centimetersToInches(16); // -> 6.29921
```

---

##  totalSecondsConverter

Écris une fonction appelée **`totalSecondsConverter`** qui prend des heures, des minutes et des secondes, puis retourne le total en secondes.

 *Note : si les minutes dépassent 60, ne fais pas de conversion supplémentaire.*

```javascript
function totalSecondsConverter(hours, minutes, seconds) {
  // Write your code here
}

totalSecondsConverter(0, 3, 4); // -> 184
totalSecondsConverter(1, 0, 7); // -> 3607
totalSecondsConverter(1, 1, 5); // -> 3665
```

---

##  billTotal

Écris une fonction appelée **`billTotal`** qui prend un montant et retourne le total avec **9.875 % de taxe** et **20 % de pourboire**.

```javascript
function billTotal(number) {
  // Write your code here
}

billTotal(15); // -> 19.48
billTotal(20); // -> 23.975
```

---

##  convertToKilometers

Écris une fonction appelée **`convertToKilometers`** qui prend une distance en miles et la convertit en kilomètres.

*1 mile ≈ 1.60934 km*

```javascript
function convertToKilometers(miles) {
  // Write your code here
}

convertToKilometers(5); // -> 8.0467
convertToKilometers(9); // -> 14.48406
```

---


